import java.awt.*;

public class word {
    String fontText;
    Color fontColour;
    int fontSize;

    public word(String text, Color colour, int size){
        fontText = text;
        fontColour = colour;
        fontSize = size;
    }
}
